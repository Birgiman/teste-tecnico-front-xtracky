services:
  api:
    image: node:20-alpine
    container_name: xtracky-mock-api
    working_dir: /data
    command: npx --yes json-server@0.17.4 --watch db.json --host 0.0.0.0 --port 3001
    ports:
      - "3001:3001"
    volumes:
      - ./db.json:/data/db.json
    restart: unless-stopped
